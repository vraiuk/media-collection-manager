// Stub — full implementation in Task 10
import type { AppDispatch } from '@app/store'

export const thumbnailRuntime = {
  cancel: (_id: string) => {},
  cleanup: (_id: string) => {},
  generate: (_id: string, _file: File, _dispatch: AppDispatch) => {},
}
